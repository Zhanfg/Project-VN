旁白:Technical Presentation Layer Smoke Test。;
tech:show -kind=code -src=code/blink -placement=right -step=0;
旁白:这是 CodeView 的第一步。;
tech:step -step=2;
旁白:现在只强调真正改变输出状态的两行。;
tech:show -kind=terminal -src=terminal/build -placement=right -step=1;
旁白:TerminalView 将命令、标准输出和错误分开呈现。;
tech:show -kind=algorithm -src=algorithm/binary-search -placement=center -step=0;
旁白:算法视图使用预先写好的 Trace。;
tech:step -step=2;
旁白:最后只剩一个候选位置。;
tech:show -kind=plot -src=plot/pwm -placement=right -step=1;
旁白:PlotView 用于波形、实验数据和函数关系。;
tech:show -kind=diagram -src=diagram/debug-loop -placement=center -step=3;
旁白:DiagramView 用来表现状态机、流程、架构和调试方法。;
tech:show -kind=circuit -src=circuit/esp32-led -placement=center -step=0;
旁白:CircuitView 保留元件和网络语义。;
tech:step -step=2;
旁白:现在聚焦 LED 与回流路径。;
tech:show -kind=instrument -src=instrument/gpio-debug -placement=right -step=0;
旁白:先用万用表看直流电平。;
tech:step -step=1;
旁白:再用示波器把电压展开到时间轴。;
tech:step -step=2;
旁白:最后用逻辑分析仪只看数字时序。;
tech:show -kind=protocol -src=protocol/i2c-whoami -placement=center -step=0;
旁白:ProtocolView 从寻址开始解释 I²C 读寄存器。;
tech:step -step=2;
旁白:真正的数据在重新寻址为读之后返回。;
tech:hide;
旁白:技术层关闭后继续普通视觉小说演出。;
