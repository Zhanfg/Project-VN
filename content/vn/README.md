# VN metadata

`registry.json` is intentionally allowed to stay empty until the plot structure is ready. Add stable IDs first; titles, route links, targets, unlock rules, tags, and custom metadata can be filled in later.
